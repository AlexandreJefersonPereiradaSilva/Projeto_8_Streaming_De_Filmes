!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Rat_filmes</title>

  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">

  <style>
    body {
      background: linear-gradient(180deg, #0b0b0e 0%, #1c1c24 100%);
      color: #f1f1f1;
      font-family: 'Poppins', sans-serif;
      min-height: 100vh;
    }

    header {
      background-color: #2b2b38;
      box-shadow: 0 4px 15px rgba(0,0,0,0.3);
      padding: 1rem 0;
      text-align: center;
    }

    header h1 {
      color: #ffcc70;
      font-weight: 700;
      letter-spacing: 2px;
    }

    header p {
      color: #ccc;
      margin: 0;
      font-size: 0.9rem;
    }

    .movie-card {
      background: #20202b;
      border: 1px solid rgba(255,255,255,0.1);
      border-radius: 10px;
      transition: transform 0.2s ease, box-shadow 0.3s ease;
    }

    .movie-card:hover {
      transform: translateY(-5px);
      box-shadow: 0 6px 20px rgba(0,0,0,0.5);
    }

    .movie-card h5 {
      color: #ffcc70;
      font-weight: 600;
    }

    footer {
      background-color: #2b2b38;
      text-align: center;
      padding: 1rem 0;
      color: #bbb;
      font-size: 0.9rem;
      margin-top: 2rem;
    }
  </style>
</head>

<body>
  <header>
    <h1>🎬 Rat_filmes 🐀</h1>
    <p>Um catálogo de filmes cheios de aventuras e roedores!</p>
  </header>

  <main class="container py-5">
    <div class="row g-4 justify-content-center">
      <!-- Loop Jinja para exibir os filmes -->
      {% for id, filme in filmes.items() %}
      <div class="col-12 col-sm-6 col-md-4 col-lg-3">
        <div class="movie-card p-3 h-100 d-flex flex-column justify-content-between">
          <div>
            <h5 class="text-center">{{ filme.titulo }}</h5>
            <p class="text-center text-secondary mb-1">Ano: {{ filme.Ano }}</p>
            <p class="text-center text-muted small">ID: {{ filme.id }}</p>
          </div>
          <div class="text-center mt-2">
            <a href="/filmes" class="btn btn-warning btn-sm">Ver JSON</a>
          </div>
        </div>
      </div>
      {% endfor %}
    </div>
  </main>

  <footer>
    © 2025 Rat_filmes — Desenvolvido com Flask 🐀🔥
  </footer>

  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>